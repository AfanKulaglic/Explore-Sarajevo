'use client'

/**
 * @deprecated Import `PostHogProvider` from `@/app/providers` (App Router pattern).
 * Kept as an alias so older imports keep working.
 */
export { PostHogProvider as AnalyticsProvider, PostHogProvider } from '@/app/providers'
